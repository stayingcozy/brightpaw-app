
export default function CheckoutSuccess() {
    // const url = window.location.href;
    // const sessionId = new URL(url).searchParams.get('session_id'); // {sessionId}
    return (
        <h3> Congrats on your new item </h3>
    )
}